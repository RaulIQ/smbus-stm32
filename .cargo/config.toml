[target.thumbv7m-none-eabi]
# Change chip if you use another F103 variant (probe-rs chip list | grep F103)
runner = "probe-rs run --chip STM32F103C8"
rustflags = ["-C", "link-arg=-Tlink.x", "-C", "link-arg=-Tdefmt.x"]

[build]
target = "thumbv7m-none-eabi"

[env]
DEFMT_LOG = "info"
